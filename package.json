{
  "name": "lexis-mollis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "platform/site"
  ],
  "scripts": {
    "build": "npm run build --workspace lexis-mollis-site",
    "deploy": "wrangler deploy",
    "dev:site": "npm run dev --workspace lexis-mollis-site",
    "preview:site": "npm run preview --workspace lexis-mollis-site"
  },
  "devDependencies": {
    "wrangler": "4.106.0"
  }
}
