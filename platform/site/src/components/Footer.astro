<footer class="site-footer">
  <div class="container footer-grid">
    <div>
      <strong>Lexis Mollis</strong>
      <p>
        Corpus ouvert de droit souple, pipeline OCR auditable, graphe de connaissances et
        exports reproductibles.
      </p>
    </div>
    <div>
      <strong>Attribution</strong>
      <p>Mohammed-Karim Khaldi & Reda Rostane. Données dérivées sous CC-BY-4.0.</p>
    </div>
    <div>
      <strong>Transparence</strong>
      <p>
        Les pages faibles restent visibles avec leurs scores, drapeaux de révision et
        provenance.
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: 72px;
    border-top: 1px solid var(--line);
    padding: 42px 0;
    color: var(--muted);
    background: rgba(255, 253, 248, 0.5);
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 24px;
  }

  strong {
    color: var(--ink);
  }

  p {
    margin: 0.5rem 0 0;
    max-width: 62ch;
  }

  @media (max-width: 760px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
